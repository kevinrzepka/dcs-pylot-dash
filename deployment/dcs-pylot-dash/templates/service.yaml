apiVersion: v1
kind: Service
metadata:
  name: dcs-pylot-dash-service
  labels:
    app: dcs-pylot-dash
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.container.port }}
      protocol: TCP
  selector:
    app: dcs-pylot-dash
